<%= form_with(model: store) do |form| %>
  <% if store.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(store.errors.count, "error") %> prohibited this store from being saved:</h2>

      <ul>
        <% store.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :store_loc, style: "display: block" %>
    <%= form.text_field :store_loc %>
  </div>

  <div>
    <%= form.label :store_num, style: "display: block" %>
    <%= form.number_field :store_num %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
